version: "3"
services:
  codes:
    build:
      context: django
      dockerfile: Dockerfile
    command: gunicorn codes.wsgi:application --bind 0.0.0.0:8000
    volumes:
      - ./codes:/usr/src/app/
    ports:
      - 8000:8000
    depends_on:
      - db
    networks:
      - database_network
    env_file:
      - .env.codes

  verification:
    build:
      context: django
      dockerfile: Dockerfile
    command: gunicorn verification.wsgi:application --bind 0.0.0.0:8000
    volumes:
      - ./verification:/usr/src/app/
    ports:
      - 8001:8001
    depends_on:
      - db
    networks:
      - database_network
    env_file:
      - .env.verification

  locations:
    build:
      context: django
      dockerfile: Dockerfile
    command: gunicorn locations.wsgi:application --bind 0.0.0.0:8000
    volumes:
      - ./locations:/usr/src/app/
    ports:
      - 8002:8002
    depends_on:
      - db
    networks:
      - database_network
    env_file:
      - .env.locations

  db:
    build:
      context: django
      dockerfile: Dockerfile
    networks:
      - database_network
    env_file:
      - .env.db

networks:
  database_network:
    driver: bridge
